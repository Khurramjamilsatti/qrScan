# Copy to .env in the project root (used by docker-compose.prod.yml).
# DB_PASSWORD must match backend/.env.docker

DB_PASSWORD=change-me-in-production
APP_URL=http://localhost:8080
FRONTEND_URL=http://localhost:8080
